name: Deploy QRadar Extension

on:
  push:
    paths:
      - "rules/*.json"
      - "manifest.xml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create Extension Zip
        run: |
          # We use -j (junk paths) to ensure manifest.xml and all JSON rules 
          # are placed at the root of the ZIP file, which prevents 422 errors.
          zip -j extension.zip manifest.xml rules/*.json

      - name: Upload Extension to QRadar
        run: |
          echo "Uploading extension to QRadar Extension Management..."
          RESPONSE=$(curl -k -s -o /dev/null -w "%{http_code}" -X POST "https://${{ secrets.QRADAR_HOST }}/api/config/extension_management/extensions" \
            -H "SEC: ${{ secrets.QRADAR_API_TOKEN }}" \
            -F "file=@extension.zip")
          
          echo "Upload Status: $RESPONSE"
          if [ "$RESPONSE" == "201" ] || [ "$RESPONSE" == "200" ]; then
            echo "Success: Extension uploaded to QRadar."
          else
            echo "Error: Received HTTP $RESPONSE. Ensure manifest.xml is correct and no duplicate extension exists."
            exit 1
          fi

      - name: Trigger Deploy
        if: success()
        run: |
          echo "Triggering global deployment..."
          curl -k -X POST "https://${{ secrets.QRADAR_HOST }}/api/config/deploy" \
            -H "SEC: ${{ secrets.QRADAR_API_TOKEN }}"
